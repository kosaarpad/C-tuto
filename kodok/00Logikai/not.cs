using System;
class Program
	{
    static void Main()
		{
		bool A = false;
		Console.WriteLine("Ha az A=" + A + " akkor !A = "+!A);
		A = true;
		Console.WriteLine("Ha az A=" + A + " akkor !A = "+!A);
		}
	}
